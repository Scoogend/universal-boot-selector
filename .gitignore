/target
**/target
node_modules/

# Note : `crates/bootsel-ui/dist/` n est PAS un repertoire de build.
# C est le frontend lui-meme, ecrit a la main et versionne.
*.log
.DS_Store
Thumbs.db

# Rien de sensible ne doit jamais entrer ici de toute facon :
# l'application ne manipule aucun secret.

# Paquets livrables : produits par la CI, publies dans les Releases.
livraison/

# Binaires embarques par le bundler : produits a la compilation.
crates/bootsel-ui/binaries/
